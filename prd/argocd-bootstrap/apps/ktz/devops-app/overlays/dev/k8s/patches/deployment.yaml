apiVersion: apps/v1
kind: Deployment
metadata:
  name: kartoza-devops-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kartoza-devops-app
  template:
    metadata:
      labels:
        app: kartoza-devops-app

