apiVersion: v1
kind: Service
metadata:
  name: kartoza-devops-app
  namespace: kartoza-devops
spec:
  selector:
    app: kartoza-devops-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
  type: LoadBalancer
